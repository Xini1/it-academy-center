name: "Setup repositories"

on:
  workflow_dispatch:
jobs:
  clone-repositories:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: kotlin ./CloneRepositories.main.kts